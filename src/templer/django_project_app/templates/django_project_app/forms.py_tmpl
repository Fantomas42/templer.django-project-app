"""Forms for ${project_root}.${egg}"""
from django import forms
from django.utils.translation import ugettext as _

from ${project_root}.${egg}.models import ${model}


class ${model}Form(forms.ModelForm):
    """Form for ${model}"""

    class Meta:
        model = ${model}
